- name: install packages
  hosts: local
  connection: local
  tasks:
  - name: install packages
    ansibible.builtin.package:
       name: "{{ item }}"
       state: present
    loop: #list of string
    - mysql
    - nginx
    - zip